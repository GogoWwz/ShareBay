class Person {
    private sex
    public name
    public constructor(name: string, sex: number) {
        this.name = name
    }
}

let p1 = new Person('wu weizhen', 1)
console.log(`p1的名字——${p1.name}`)
